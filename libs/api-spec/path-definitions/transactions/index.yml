get:
  tags: [Transactions]
  responses:
    '200':
      description: OK
      content:
        application/json:
          schema:
            type: array
            items: 
              $ref: ../../schemas/Transaction.yml#/Transaction
    '500':
      description: Server Error
